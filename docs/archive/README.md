# Archive

One-off scaffolding kept for provenance, not for reuse. Nothing here is
maintained, and nothing here is a current description of the codebase — read
`CLAUDE.md` and the live docs for that.

These four files sat in a `git stash` for months. They are recorded here so the
finished documents on `main` have a traceable origin.

| File | What it is |
|---|---|
| `audit-instructions.md` | Read-only audit rubric, scoped to this repo. |
| `audit-report-2026-07-02.md` | The resulting audit, against `main` @ `daa2551` (v0.10.0). Findings are either fixed or tracked as issues; the file is a snapshot, not a to-do list. |
| `fable-codebase-audit.prompt.md` | The brief that produced `docs/codebase-audit.md`. |
| `fable-desktop-transition-investigation.prompt.md` | The brief that produced `docs/desktop-transition-investigation.md`. |
